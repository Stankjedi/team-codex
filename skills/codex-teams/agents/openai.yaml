interface:
  display_name: "Codex Teams"
  short_description: "Codex CLI swarm panes + real-time team bus for lead + worker-N + utility sessions"
  default_prompt: "Use $codex-teams with the topology (lead, worker-N, utility-1). Keep lead orchestration-only, delegate dynamically to worker pool, enforce iterative question/answer collaboration, then handoff to utility for git push/merge with evidence."
