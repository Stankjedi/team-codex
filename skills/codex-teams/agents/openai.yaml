interface:
  display_name: "Codex Teams"
  short_description: "Codex multi-agent bus for lead(external) + reviewer-1 + worker-1/2/3 sessions"
  default_prompt: "Use $codex-teams with topology (lead external, reviewer-1, worker-1/2/3). Lead plans/orchestrates, workers implement, reviewer performs review-only pass, then lead compares both reviews and re-delegates fixes until clean."
